#!/usr/bin/python3
# @a_idk scripting

# function that returns the number of keys in a dictionary


def number_keys(a_dictionary):
    # initialing variables and setting the list
    keys_lst = list(a_dictionary.keys())
    keys_num = 0
    # counting the keys
    for x in key_lst:
        keys_num = keys_num + 1
    return (keys_num)
