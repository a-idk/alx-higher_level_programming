#!/usr/bin/python3

# @a_idk scripting

''' function that  does exactly the same as the following Python bytecode:'''


def magic_calculation(a, b, c):
    if a < b:
        return (c)
    if c > b:
        return (a + b)
